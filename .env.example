# Copy this file to .env and fill values
# The script Scripts/RainDeployOB.sh expects this file at the repository root

# Required: Client identifier (lowercase, 3-15 chars, letters/numbers only)
CLIENT_NAME=acme

# Required: AWS region for primary deployment (where stacks/buckets are created)
RUNTIME_REGION=us-east-1

# Required: AWS CLI profile to use for deployment
RUNTIME_PROFILE=default

# Required: Client's management account ID (for organization deployments)
CLIENT_MGMT_ACCOUNT_ID=111122223333

# Required: Cyngular AWS account ID (to assume readonly role)
CYNGULAR_ACCOUNT_ID=851565895544

# if using AWS Organizations
ORGANIZATION_ID=o-xxxxxxxxxx
# Comma-separated OU IDs. Use root OU ID (r-xxxx) for org-wide deployment
ORGANIZATIONAL_UNIT_IDS=ou-xxxx-xxxxxxxx

# Optional: Existing CloudTrail bucket name (if already configured)
CloudTrailBucket=""

# Feature toggles [see CFN/Services.yaml]
# Enable CloudTrail creation (true/false)
EnableCloudTrail="true"

# Enable DNS logging service (true/false/bucket-name)
EnableDNS="true"

# Enable EKS audit logging (true/false)
EnableEKS="true"

# Enable VPC Flow Logs (true/false)
EnableVPCFlowLogs="true"

# Enable bucket policy manager for cross-account log access (true/false)
EnableBucketPolicyManager="true"

# Increment to retrigger ServiceManager Lambda from CloudFormation stack
ServiceManagerOverride=1

# Optional: Comma-separated list of regions to exclude from service manager lookup
ExcludedRegions="eu-west-3,ap-south-2"

# StackSet operation preferences (tuning)
# Percentage of accounts that can fail before stopping deployment (0-100)
FAILURE_TOLERANCE_PERCENTAGE=25

# Maximum percentage of accounts to deploy to concurrently (1-100)
MAX_CONCURRENT_PERCENTAGE=90
