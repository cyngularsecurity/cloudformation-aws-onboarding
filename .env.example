# Copy this file to .env and fill values
# The script Scripts/RainDeployOB.sh expects this file at the repository root

# Required identifiers
CLIENT_NAME=acme
RUNTIME_REGION=us-east-1
RUNTIME_PROFILE=default

# Cyngular
CYNGULAR_ACCOUNT_ID=851565895544

# Organizations identifiers
ORGANIZATION_ID=o-xxxxxxxxxx
# Comma-separated OU IDs (no spaces). Example: ou-abcd-11111111,ou-abcd-22222222
ORGANIZATIONAL_UNIT_IDS=ou-xxxx-xxxxxxxx

# Optional features/resources
ExcludedRegions="eu-west-3,ap-south-2"
CloudTrailBucket=

# Feature toggles
EnableDNS=true
EnableEKS=true
EnableVPCFlowLogs=true
EnableBucketPolicyManager=true
ServiceManagerOverride=1

# Management account IDs
# Used by region-scoped stacks (STACK_PARAMS)
ClientMgmtAccountId=
# Used by StackSets parameters (note casing difference)
CLIENT_MGMT_ACCOUNT_ID=

# StackSet operation preferences (tuning)
FAILURE_TOLERANCE_PERCENTAGE=25
MAX_CONCURRENT_PERCENTAGE=50
